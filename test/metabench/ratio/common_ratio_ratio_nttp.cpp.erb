#include "ratio_nttp.h"

<% (1..n).each do |i| %>
  struct test<%= i %> {
    static constexpr units::ratio r1{<%= i %>, <%= n + 1 - i %>};
    static constexpr units::ratio r2{<%= n + 1 - i %>, <%= i %>};

#if defined(METABENCH)
    static constexpr units::ratio r3 = units::common_ratio(r1, r2);
#else
    static constexpr bool r3 = true;
#endif
  };
<% end %>


int main()
{
}
