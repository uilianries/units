#include "ratio_value.h"

<% (1..n).each do |i| %>
  struct test<%= i %> {
    static constexpr units::ratio r1{<%= i %>, <%= n + 1 - i %>};
    static constexpr units::ratio r2{<%= n + 1 - i %>, <%= i %>};

#if defined(METABENCH)
    static constexpr units::ratio r3 = r1 * r2;
    static constexpr units::ratio r4 = r1 / r2;
#endif
  };
<% end %>


int main()
{
}
